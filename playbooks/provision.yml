- name: "ansible-playbook-bundle-template playbook to provision the application"
  hosts: localhost
  gather_facts: false
  connection: local
  vars:
    apb_action: provision
  roles:
    - role: ansible-playbook-bundle-template
